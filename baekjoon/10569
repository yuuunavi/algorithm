#include <iostream>
using namespace std;
int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    int n, v, e, ans;
    cin >> n;
    for(int i=0; i<n; i++){
        cin >> v >> e;
        ans = 2 - v + e;
        cout << ans << endl;
    }
    return 0;
}
