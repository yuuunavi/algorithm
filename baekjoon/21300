#include <iostream>
using namespace std;
int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    int n, sum=0;
    for(int i=0; i<6; i++){
        cin >> n;
        sum += n;
    }
    cout << sum*5 << endl;
    return 0;
}
