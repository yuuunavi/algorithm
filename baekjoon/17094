#include <iostream>
using namespace std;
int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    int n, two=0, e=0;
    string s;
    cin >> n >> s;
    for(int i=0; i<n; i++){
        if(s[i] == '2') two++;
        else e++;
    }
    if(two > e) cout << "2" << endl;
    else if(two < e) cout << "e" << endl;
    else cout << "yee" << endl;
    return 0;
}
