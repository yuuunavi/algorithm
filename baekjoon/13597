#include <iostream>
using namespace std;

int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    int a, b, c;
    cin >> a >> b;
    if(a == b) c = a;
    else {
        if(a > b) c = a;
        else c = b;
    }
    cout << c << endl;
    return 0;
}
