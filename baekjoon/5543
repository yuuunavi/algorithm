#include <iostream>
#include <algorithm>
using namespace std;
int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    int a[3];
    int b[2];
    cin >> a[0] >> a[1] >> a[2];
    cin >> b[0] >> b[1];
    sort(a, a+3);
    sort(b, b+2);
    cout << a[0] + b[0] - 50 << endl;
    return 0;
}
