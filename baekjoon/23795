#include <iostream>
using namespace std;
int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    int n, sum = 0;
    while(1){
        cin >> n;
        if(n == -1) break;
        sum += n;
    }
    cout << sum << endl;
    return 0;
}
