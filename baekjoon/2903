#include <iostream>
using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int n;
	cin >> n;
	int d = 4;
	int b = 1;

	for (int i = 1; i <= n; i++) {
		b *= 2;
	}
	
	d = (b + 1) * (b + 1);

	cout << d << endl;
	return 0;
}
