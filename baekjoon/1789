#include <iostream>
using namespace std;

int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    long long s, n=0;
    cin >> s;
    while(s>0){
        n++;
        s -= n;
    }
    if(s<0) n--;
    cout << n << endl;
    return 0;
}
