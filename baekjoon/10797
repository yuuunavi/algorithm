#include <iostream>
using namespace std;
int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    int n, t;
    int car[10] = {0};
    cin >> n;
    for(int i=0; i<5; i++){
        cin >> t;
        car[t]++;
    }
    cout << car[n] << endl;
    return 0;
}
