#include <iostream>
using namespace std;

int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    string s;
    cin >> s;
    if(s.substr(0,3) == "555") cout << "YES" << endl;
    else cout << "NO" << endl;
    return 0;
}
