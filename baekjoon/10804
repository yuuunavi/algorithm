#include <iostream>
#include <string>
using namespace std;

void swap(int arr[], int a, int b)
{
    int x;
    while(1){
        swap(arr[a], arr[b]);
        x = b - a;
        if(x <= 1) break;
        a++;
        b--;
    }
}

int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    int arr[20];
    int a, b;

    for(int i=0; i<20; i++){
        arr[i] = i+1;
    }
    for(int i=0; i<10; i++){
        cin >> a >> b;
        swap(arr, a-1, b-1);
    }
    for(int i=0; i<20; i++){
        cout << arr[i] << " ";
    }
    cout << endl;
    return 0;
}
