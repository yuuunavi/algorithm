#include <iostream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);

	int a, b, c;
	cin >> a >> b >> c;
	if (a + b + c >= 100) cout << "OK" << endl;
	else {
		if (a <= b && a <= c) cout << "Soongsil";
		if (b <= a && b <= c) cout << "Korea";
		if (c <= a && c <= b) cout << "Hanyang";
	}
	return 0;
}
