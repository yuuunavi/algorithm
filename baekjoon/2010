#include <iostream>
using namespace std;
int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    int n, t;
    int r = 0;
    cin >> n;
    for(int i=0; i<n; i++){
        cin >> t;
        r += t;
    }
    cout << r - n + 1 << endl;
    return 0;
}
